# Require real-time shared session parity

Offdex must support real-time shared session visibility between Codex desktop, Offdex Mobile, and Offdex WebUI. Sessions started from any surface should be observable from the others with no meaningful delay, preserving thread identity and catching up after reconnect without duplicate items; this is a core performance and user-experience requirement rather than a handoff feature.
