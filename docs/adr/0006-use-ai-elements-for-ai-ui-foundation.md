# Use AI Elements for AI UI foundation

Offdex will use AI Elements as the frontend foundation for AI conversation surfaces, including messages, conversation structure, tool displays, and prompt input patterns where the library fits. Components should be adapted toward the Codex-faithful visual language and platform-native mobile behavior rather than used as generic chatbot defaults.
